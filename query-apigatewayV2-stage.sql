SELECT
    accountId,
    arn,
    availabilityZone,
    awsRegion,
    configuration.accessLogSettings.destinationArn,
    configuration.accessLogSettings.format,
    configuration.autoDeploy,
    configuration.createdDate,
    configuration.defaultRouteSettings.dataTraceEnabled,
    configuration.defaultRouteSettings.detailedMetricsEnabled,
    configuration.defaultRouteSettings.loggingLevel,
    configuration.defaultRouteSettings.throttlingBurstLimit,
    configuration.defaultRouteSettings.throttlingRateLimit,
    configuration.deploymentId,
    configuration.description,
    configuration.lastUpdatedDate,
    configuration.stageName,
    configurationItemCaptureTime,
    configurationItemStatus,
    configurationStateId,
    relationships.resourceId,
    relationships.resourceName,
    relationships.resourceType,
    resourceCreationTime,
    resourceId,
    resourceName,
    resourceType,
    version
WHERE
    resourceType = 'AWS::ApiGatewayV2::stage'